import { put, takeEvery, takeLatest, delay, all, call } from 'redux-saga/effects'


export default function* rootSaga() {
    /*yield all([
        yield takeEvery('INCREMENT_ASYNC', incrementAsync),
        yield takeEvery('INCREMENT_ASYNC_N', incrementAsyncN),
    ])*/
}


